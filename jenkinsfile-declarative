pipeline{
    agent any
    environment{
        project= "Test"
    }
    stages{
        stage('Build'){
            steps{
                sh "sh script.sh" // lets hope it runs
            }
        }
        stage('Notify'){
            steps{
                sh "echo this file runs succesfully"
            }
        }
    }
    post{
        success{
            echo "success"
        }
        failure{
            echo "failed"
        }
        always{
            deletedir()
        }
    }
}
